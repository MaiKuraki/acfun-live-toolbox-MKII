## 待补全与修正
- 新增弹幕端点文档：
  - `POST /api/acfun/danmu/start`（body: liverUID；成功/参数缺失示例）
  - `POST /api/acfun/danmu/stop`（body: sessionId；成功/参数缺失示例）
  - `GET /api/acfun/danmu/room-info`（query: liverUID；成功/参数缺失示例）
- 修正 `GET /api/acfun/user/info` 参数名为 `userId`（字符串），与代码的 `handleUserEndpoints` 保持一致；保留 `live/user-info` 的数值 `userID` 说明

## 实施
- 编辑 `docs/apidoc-src/endpoints.apidoc.js` 新增弹幕端点与成功/失败示例；将 `user/info` 的 `@apiQuery` 改为 `userId`
- 生成文档并验证页面展示

## 依据
- 代码位置：`packages/main/src/server/AcfunApiProxy.ts:296–366`（danmu）；`140–166` 与 `386–409`（user/info）
- 保证所有示例与错误文案来源于实现（如 `liverUID is required`、`sessionId is required`）