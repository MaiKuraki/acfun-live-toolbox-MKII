## 布局与样式
- 去掉多余的直播日期筛选，仅保留一个 `<t-date-picker>`（现有表单内的这一处）。
- 将字段分两行、各占 50% 宽度：
  - 第 1 行：直播日期（左 50%）、类型（右 50%）
  - 第 2 行：用户（左 50%）、关键词（右 50%）
- 统一控件宽度：使用容器样式控制 50% 布局，不再使用 240px 固定宽度；在小屏下自动换行。
- 将“查询”和“导出”按钮移至页面右上方房间 `<t-select>` 右侧，并在其左侧添加文本“当前房间：”。

## 字段与数据模型调整
- 将“关键词1”重命名为“关键词”，表单模型从 `formData.keyword1` 改为 `formData.keyword`；查询参数统一传 `q=formData.keyword.trim()`。
- 保持类型传逗号分隔 `type=a,b,c`，用户筛选为单选 `user_id`，日期为单日范围（`from_ts/to_ts`）。

## 组件与样式修改位置（文件：packages/renderer/src/pages/LiveDanmuPage.vue）
- 页面 header：
  - 在选择器左侧插入文本“当前房间：”。
  - 将“查询”“导出”按钮从表单内移至 header 中选择器右侧（原 header actions 容器）。
- 表单结构：
  - 调整 `<t-form>` 项顺序与布局为两行 50%（通过外层容器 class + CSS `flex-basis: 50%` 控制）。
  - 移除多余的日期控件（若存在），保留当前表单内唯一日期控件。
  - 重命名关键词字段并更新所有引用（包括 `onSubmit` 组装参数）。
- CSS：
  - 新增表单行分隔与 50% 布局样式（双列栅格）。
  - 保持表格区域固定高度 400px 与分页区独立显示；内容列省略与“查看详情”保持不变。

## 兼容性与行为
- 仍以“查询”按钮作为显式触发请求，避免频繁输入导致多次请求；房间切换时自动查询。
- 遮罩加载保持当前实现，查询期间覆盖表格区域。

## 验证
- 调整后页面在桌面宽度下两行四列布局；在小屏宽度下自动换行但字段顺序不变。
- “查询”“导出”按钮位于右上选择器右侧；选择器左侧显示“当前房间：”。
- 类型、关键词、用户、日期筛选均生效；分页与总数正常更新。
- 通过类型检查与构建验证无错误。