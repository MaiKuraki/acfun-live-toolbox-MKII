## 页面改动（LiveRoomPage.vue）
1. 隐藏按钮：当 `room.isLive === false` 时，不显示 `连接采集` 与 `进入直播间` 两个按钮（现位于 `packages/renderer/src/pages/LiveRoomPage.vue:192–216`）。
2. 删除按钮：移除房间卡片操作区中的 `查看弹幕` 按钮（`packages/renderer/src/pages/LiveRoomPage.vue:202–208`），仅保留下拉菜单同名项。
3. 个人空间链接：在主播 UID 展示处，将 UID 由 `room.liveId` 更正为 `room.liverUID`，并追加一个 `个人空间` 链接，点击通过 `window.electronApi.system.openExternal('https://www.acfun.cn/u/'+room.liverUID)` 打开（修改 `packages/renderer/src/pages/LiveRoomPage.vue:163–165`）。
4. 下拉菜单新增“房间设置”：在 `getRoomMenuOptions(room)` 中新增 `房间设置` 项（`packages/renderer/src/pages/LiveRoomPage.vue:526–543`），点击打开设置对话框，提供两个开关：
   - 是否自动连接采集弹幕（映射 `room.autoConnect`）
   - 是否开播通知（新字段 `room.notifyOnLiveStart`）

## 设置对话框（LiveRoomPage.vue）
- 新增 `t-dialog`：`showSettingsDialog`、`settingsRoomId`、`settingsForm`（包含 `autoConnect` 与 `notifyOnLiveStart`）。
- 读写：读取当前房间的设置到表单；保存时调用 `roomStore.updateRoomSettings(roomId, { autoConnect, notifyOnLiveStart })` 并持久化；关闭后刷新房间列表以反映状态。

## 启动行为调整（packages/renderer/src/stores/room.ts）
1. Room 类型扩展：在 `Room` 接口新增 `notifyOnLiveStart?: boolean` 并在本地存储持久化读写（更新 `readRoomsFromStorage` 与 `saveRoomsToStorage` 映射，`packages/renderer/src/stores/room.ts:39–40、386–431`）。
2. 自动连接逻辑改造（`loadRooms`）：
   - 先执行一次 `await refreshRoomStatus()`，用真实接口获取每个房间的 `isLive` 与采集状态。
   - 仅当房间 `autoConnect === true && isLive === true` 时调用 `window.electronApi.room.connect(room.id)`。
3. 启动时开播通知：在上述首次刷新后，比较“刷新前的 `isLive`”与“刷新后的 `isLive`”，对满足下列条件的房间触发通知：
   - `notifyOnLiveStart === true`
   - 由 `false -> true`（未直播变为直播中）
   - 排除自己：若 `useAccountStore().userInfo?.userID` 与房间 `liverUID` 相同则不通知
   - 调用浏览器窗口通知接口：`new Notification('开播提醒', { body: '主播xxx已开播' })`

## 现有逻辑对齐与兼容
- UI 文案与状态：继续使用现有 `room.isLive` 展示“直播中/未直播”，采集状态用现有 `collectStatusLabel(room.status)`。
- 主进程无需改动：连接/断开通过现有 `preload` 的 `room.connect/room.disconnect/room.details/status` IPC；弹幕采集仍由 `RoomManager` 与 `AcfunAdapter` 驱动（参考 `packages/main/src/rooms/RoomManager.ts:186–209`）。

## 验证与安全
- 仅静态代码检查与类型检查：不启动渲染进程开发服务器；验证 `Room` 新字段的存取、设置对话框的开关读写、按钮显隐逻辑。
- UI 验证点：
  - 未直播房间看不到“连接采集/进入直播间”按钮；仍可通过菜单进入“查看弹幕”。
  - UID 旁新增“个人空间”链接，能打开正确用户页。
  - 房间设置对话框可开关“自动连接采集弹幕/开播通知”，并持久化。
  - 程序启动后：已开启“自动连接采集弹幕”的房间，若“直播中”则自动连接；开启“开播通知”的房间在首次刷新时由未直播变为直播中会弹系统通知，并排除自身 UID。

## 代码定位参考
- 房间页面：`packages/renderer/src/pages/LiveRoomPage.vue:192–216、202–208、163–165、526–543`
- 房间存储：`packages/renderer/src/stores/room.ts:39–40、160–172、181–248、386–431`
- 账户信息：`packages/renderer/src/stores/account.ts:44–77、279–286` 用于获取当前用户 UID 排除自身。

确认后我将按上述方案修改相关文件并进行静态验证。