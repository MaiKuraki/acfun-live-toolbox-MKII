## 修改点
- 仅在 `room.isLive` 为真时显示采集按钮。
- 采集按钮文案随状态切换：
  - `connected` → “断开采集”
  - `connecting` → “连接采集中”（按钮禁用）
  - 其他 → “连接采集”
- 进入直播间按钮继续在非离线时显示。

## 文件
- packages/renderer/src/pages/LiveRoomPage.vue

## 验证
- 类型检查通过；直播中显示正确按钮与文案，连接中置灰，未直播隐藏采集按钮。