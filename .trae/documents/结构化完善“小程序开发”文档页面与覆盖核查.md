## 页面目标
- 全面且结构化呈现：实现原理、架构（overlay/window/ui/index.js）、接口（主进程与 AcFun 代理）、事件（生命周期与订阅）、消息传输，以及两个随仓库样例。
- 页面不再是纯文本：使用卡片、折叠、表格与可复制代码示例；顶部锚点导航与返回顶部。

## 覆盖核查（已确认项与引用）
- 主进程接口（示例引用）：
  - 事件查询：`GET /api/events`（packages/main/src/server/ApiServer.ts:237）
  - 事件统计：`GET /api/stats/events`（packages/main/src/server/ApiServer.ts:283）
  - 导出：`GET/POST /api/export`（packages/main/src/server/ApiServer.ts:293, 314）
  - Overlay REST：`GET /api/overlay/:overlayId`（packages/main/src/server/ApiServer.ts:710）、`POST /api/overlay/:overlayId/action`（packages/main/src/server/ApiServer.ts:1027）、`POST /api/overlay/:overlayId/send`（packages/main/src/server/ApiServer.ts:1044）、`POST /api/overlay/create`（packages/main/src/server/ApiServer.ts:1148）
  - SSE：`/sse/overlay/:overlayId`（packages/main/src/server/ApiServer.ts:751）、`/sse/plugins/:pluginId/overlay`（packages/main/src/server/ApiServer.ts:847）、`/sse/renderer/readonly-store`（packages/main/src/server/ApiServer.ts:1078）、`/sse/system/logs`（packages/main/src/server/ApiServer.ts:384）
  - 包装页：`GET /overlay-wrapper`（packages/main/src/server/ApiServer.ts:1198）
  - 插件静态托管：`/plugins/:id/ui|window|overlay[/*]` 与直达入口（packages/main/src/server/ApiServer.ts:540–576）
- AcFun 代理端点（示例分组）：
  - auth/danmu/user/live/gift/manager/badge/replay/preview/image/eventsource（packages/main/src/server/AcfunApiProxy.ts:140–206 路由分发；具体模块例如 live:450–848、danmu:296–366、badge:1113–1238）
- 架构与事件：
  - OverlayManager 事件：updated/message/closed/action（packages/main/src/server/ApiServer.ts:795–822 监听与转发）
  - 插件 overlay 频道消息结构（packages/main/src/server/ApiServer.ts:974–1018 发布逻辑）

## 页面重构方案（仅改造 ApiDocs.vue 的“小程序开发”分区）
- 顶部锚点导航：概览 / 架构 / 接口 / 事件 / 消息传输 / 样例
- 组件与布局：
  - t-card 承载每个分区；t-collapse 展示详细说明；t-table 罗列关键端点与事件；预格式化代码块 + “复制”按钮
- 分区内容：
  1) 概览卡片：小程序开发流程（真实请求、不使用 mock；只读仓库与静态托管）；manifest 关键字段简述（overlay/ui/window 声明）。
  2) 架构卡片：
     - overlay：单实例策略、静态托管入口、SSE 事件；引用 overlay-wrapper 用法
     - window：窗口声明与托管（单实例置顶）
     - ui：只读仓库订阅与总线通信、路由承载
     - index.js：init/cleanup/handleMessage 约定与示例
  3) 接口表格：
     - 主进程端点分组列出（事件、Overlay、SSE、包装页、静态托管），每行含路径、方法、用途、认证/禁用行为；提供外链到 `/docs/api/index.html`
     - AcFun 代理分组（auth/danmu/user/live/...），统一备注“详见接口文档外链”并列举常用端点
  4) 事件卡片：
     - overlay-updated/message/closed/action 与 heartbeat 事件说明；只读仓库 init/update；插件 overlay 频道消息结构（message/update/closed/action）
     - 订阅代码示例（SSE 基本订阅 + 事件分派演示）
  5) 消息传输卡片：
     - UI/Window → Overlay（REST 示例：`/api/overlay/:overlayId/send`，总线示例说明）
     - 主进程 → UI/Overlay（SSE 下行与心跳）
  6) 样例卡片：
     - sample-overlay-ui：入口路径、只读仓库订阅、日志区；代码位置：`buildResources/plugins/sample-overlay-ui/overlay.html`、`index.js`
     - sample-overlay-window：入口路径、窗口行为；代码位置：`buildResources/plugins/sample-overlay-window/window.html`、`index.js`
- 代码示例（可复制）：
  - index.js 骨架（init/cleanup/handleMessage）
  - SSE 订阅只读仓库与 Overlay 的最小示例（仅演示订阅与事件处理）
  - REST 发送消息/动作的 curl 示例
  - overlay-wrapper URL 示例（参数：plugin/type/overlayId/route/html）

## 验证与交付
- 修改并整理 ApiDocs.vue 的“小程序开发”分区为结构化页面；不更改接口实现
- 跑类型检查：`pnpm -r run typecheck`
- 人工验证：锚点跳转、复制按钮、外链与表格显示正常；内容覆盖完整。

## 注意
- 按项目现有依赖使用 tdesign 组件（已使用 `tdesign-vue-next`）
- 认证与禁用行为严格按实现描述；不添加未实现端点或字段。
- 代码引用采用 `file_path:line_number` 文本标注，便于 IDE 跳转。