## 目标
- 修复房间列表仍溢出且无滚动条：确保卡片内容区和列表容器的弹性布局与滚动生效。
- 封面加载异常：为封面图添加错误回退到默认封面，避免破图。
- 文案细化：
  - 主播名后括号改为“UID”。
  - “弹幕获取中”单独使用一个 span 展示，不与“直播中”合并。
  - 移除“房间ID”行。

## 修改
- `packages/renderer/src/pages/LiveRoomPage.vue`
  1) 样式：`.room-list-card :deep(.t-card__body)` 添加 `flex:1; overflow:hidden;`；`.room-list` 保持 `flex:1; min-height:0; overflow-y:auto;`。
  2) 模板：
     - 封面 `<img>` 添加 `@error="onCoverError"` 回退默认封面。
     - 将主播行改为 `（UID: room.liveId）`；删除“房间ID”显示行。
     - 状态区域在 `connected` 时追加独立 `<span>` 文案“弹幕获取中”。
  3) 方法：新增 `onCoverError(e)` 将目标图片替换为默认封面。

## 验证
- 类型检查通过。
- 列表超过3项时卡片内出现滚动条且不溢出；封面异常时显示默认封面；主播行显示 UID；直播中另起一行/块显示“弹幕获取中”。