## 审核结论
- 已覆盖主进程实现的所有 /api、/sse、/plugins 和包装页端点
- 不记录未实现的 /api/diagnostics，以保持文档与代码一致

## 补充项（增强细节，不改变端点集）
- Overlay REST 增加成功示例（curl+返回体）：
  - GET /api/overlay/:overlayId → 返回 overlay/room/token/websocket_endpoint 的示例
  - POST /api/overlay/:overlayId/action → 成功响应示例（{ success: true }）
  - POST /api/overlay/:overlayId/send → 成功响应示例
  - POST /api/overlay/create → 成功响应示例（含 overlayId）
- 插件 Overlay 消息中心：
  - POST /api/plugins/:pluginId/overlay/messages → 单发与广播的 curl 示例（返回 id 或 count/ids）
- 只读仓库：
  - POST /api/renderer/readonly-store → init/update 两类成功示例
- SSE 细则：
  - /sse/plugins/:pluginId/overlay → Last-Event-ID 使用示例（curl 带 Header）
- Live 权限/认证说明一致性：
  - 已对 /live/permission、/live/stream-status、/live/transcode-info 标注 401 行为；其余端点保持代码现状，不声称必须登录

## 实施
- 编辑 docs/apidoc-src/endpoints.apidoc.js，为上述端点补充 @apiSuccessExample 与 @apiExample（curl），严格以返回结构为准，不虚构字段
- 生成并验证文档：pnpm docs:api；确认页面加载正常

## 验收
- 所有新增示例可复制执行（在有服务与正确参数时）
- 文档字段与代码保持一致，无未实现端点或不存在字段