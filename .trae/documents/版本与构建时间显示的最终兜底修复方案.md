**症状**
- 关于页仍未显示版本与构建时间，说明当前 `system.getBuildInfo` 返回未命中或渲染层未正确赋值。

**兜底方案**
1. 在设置页增加硬兜底：
   - 调用 `system.getBuildInfo` 后，如果失败或返回的 `version/buildTime` 无效，使用 `fs.exists`/`fs.readFile` 直接读取根目录 `package.json`（相对路径 `package.json` 以主进程工作目录解析）
   - 从 JSON 中解析 `version`，并用 `fs.stat` 的替代逻辑（可通过新增 IPC：`system.statPath` 或在主进程扩展 `system.getBuildInfo`）获取 `mtimeMs`
   - 若 `stat` 不可用，则构建时间显示 “未知”
2. （若你同意）在主进程增加 `system.statPath` 简单返回 `mtimeMs`，配合现有 `fs.exists`/`fs.readFile`，渲染层即可稳妥获取构建时间
3. 保持 UI 紧凑样式；仅修复信息获取逻辑，不影响其他功能

**验证步骤**
- 首次进入关于页即可显示：版本=根 package.json 的 `3.1.0`，构建时间=该文件修改时间
- `system.getBuildInfo` 返回不稳定时，兜底逻辑依然正确显示

如确认，我将新增 `system.statPath` 并在设置页实现上述兜底读取逻辑，之后进行类型检查验证。