## 目标
- 删除服务器状态及头部的“在线”和版本号
- 将 Tab（可用接口/小程序开发）移动到 Header 右侧，保持双 Tab 切换
- 让 iframe 充满除 Header 外的剩余空间

## 修改点
- 文件：packages/renderer/src/pages/ApiDocs.vue
- 结构：
  - Header 左侧保留标题“开发文档”，右侧放置两个切换按钮（可用接口/小程序开发）
  - 移除服务器状态块与加载状态块
  - 内容区域：
    - “可用接口”仅保留 apidoc iframe，铺满剩余空间
    - “小程序开发”保持说明块，内容区域可滚动
- 样式：
  - 容器使用 flex 布局：header 固定，content 设为 flex:1
  - iframe 宽高 100%，父容器高度跟随 content（flex:1）

## 验证
- 运行类型检查（pnpm -r run typecheck）
- 人工验证在应用中切换 Tab 与 iframe占满效果，确认无在线/版本号文案