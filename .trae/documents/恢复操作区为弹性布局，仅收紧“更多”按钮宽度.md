## 目标
- 让“进入直播间”等按钮保持原始宽度并同行显示，仅将最后一个“更多”按钮收紧为图标大小。

## 修改
- 样式调整：
  - 将 `.room-actions` 从三列栅格改回弹性布局：`display:flex; gap:8px; align-items:center; flex-wrap:nowrap;`
  - 保留并应用 `.more-btn` 的窄宽样式：`width/min-width/height=28px; padding=0`，文本容器居中。
- 模板保持：最后一个按钮使用 `shape="square" class="more-btn"`。

## 验证
- 类型检查通过。
- 四个按钮同行；“进入直播间”不再被压缩；仅“更多”按钮为紧凑图标。