**目标**
- 版本号从 package.json（Electron app.getVersion）获取并显示
- 构建日期使用实际构建时间
- 面板与控件间距参照“弹幕管理”页面收紧

**实现步骤**
1. 版本号获取
- 复用已存在的主进程 IPC：`get-app-version`（packages/main/src/ipc/ipcHandlers.ts:46）
- 在 preload 增加桥接：`system.getAppVersion: () => ipcRenderer.invoke('get-app-version')`
- Settings.vue `onMounted` 调用并填充 `appVersion`

2. 构建日期获取
- 在主进程新增 IPC：`system.getBuildInfo`
  - `version: app.getVersion()`
  - `buildTime: 尝试读取 app.getAppPath() 下主要文件的 mtime（如 package.json 或主 bundle）；失败则回退到 Date.now()`
- 在 preload 增加桥接：`system.getBuildInfo`
- Settings.vue `onMounted` 调用填充 `buildTime`（格式为本地化日期时间）

3. 设置页面间距优化（参照 LiveDanmuPage）
- 将以下样式值调整为更紧凑：
  - `.settings-page padding: 16px`
  - `.settings-container gap: 16px`
  - `.settings-panel padding: 16px`
  - `.panel-section margin-bottom: 16px`
  - `.section-title margin-bottom: 12px`
  - `.form-group gap: 12px`
  - `.button-group gap: 8px`
- 保持现有结构与交互不变

**验证**
- 进入关于页显示的版本等于根 package.json 版本
- 构建日期显示为实际文件修改时间（非固定写死值）
- 视觉间距与“弹幕管理”页面一致（更紧凑）

确认后我将按上述步骤实现并进行类型检查。