## 目标
- 去除顶部标签样式，改为横向两端对齐：左侧为标签，右侧为开关控件。

## 修改点
- 将设置对话框表单从 `vertical` 改为 `horizontal` 布局，并移除 `label-align="top"`。
- 设定统一标签宽度避免与控件重叠：如 `labelWidth="180"`（或 200，根据观感微调）。
- 局部样式使控件靠右对齐，达到“左右两端对齐”效果，仅作用于本对话框，避免影响其他表单。

## 实施文件与位置
- 对话框模板：`packages/renderer/src/pages/LiveRoomPage.vue`
  - 现有位置：房间设置对话框（约 337 行附近）
  - 变更：
    - `<t-form :data="settingsForm" layout="horizontal" label-align="left" :label-width="180">`
    - 为该表单容器添加类名（如 `class="room-settings-form"`），并在 `<style scoped>` 增加：
      - `.room-settings-form :deep(.t-form__controls) { justify-content: flex-end; }`
      - （如需）`.room-settings-form :deep(.t-form__item) { align-items: center; }`

## 验证
- 打开“房间设置”对话框：标签固定在左侧，开关控件在右侧，未出现文本与控件重叠。
- 不影响其他页面或表单布局。

确认后我将按上述方案更新模板与样式。