## 范围
- 更新 docs 中现有文档以同步新增 HTTP 接口：/api/popup 与 /api/windows/*。
- 文件：api-reference.md（简要引用）、integration-guide.md（集成使用）、plugin-development.md（插件端实践）、apidoc-src/endpoints.apidoc.js（接口注释）。

## 变更
- 在 integration-guide.md 添加“窗口与弹窗”章节：如何在主/插件窗口使用 HTTP 接口触发弹窗与控制窗口，包含 X-Plugin-ID 说明与 cURL 示例。
- 在 plugin-development.md 添加“窗口交互（HTTP）”小节：插件路由下如何指定自身 windowId/pluginId、如何获取自身窗口 ID（GET /api/windows/self）、如何安全发起弹窗。
- 在 api-reference.md 补充“系统 HTTP”章节条目：列出 /api/popup 与 /api/windows list/self/show/focus/close 的用途（以简短概览方式）。
- 在 docs/apidoc-src/endpoints.apidoc.js 增加注释块：POST /api/popup、POST /api/windows/show、POST /api/windows/focus、POST /api/windows/close、GET /api/windows/list、GET /api/windows/self。

## 输出
- 所有文档与示例保持与代码一致，避免回退旧逻辑。
- 不新建文件，仅增补现有文档段落。