## 将进行的修改
- LiveRoomPage.vue：
  - 删除 onMounted(loadRooms)。
  - 封面仅在 coverUrl 存在时渲染 <img>；否则渲染本地占位 div；移除 @error。
  - 添加占位样式。
- LiveManagePage.vue：
  - 封面仅在 cover 存在时渲染 <img>；否则占位 div；不使用默认封面路径。
- room.ts：
  - loadRooms 不构造占位对象；对主进程返回的房间 ID 批量详情成功后再 push 房间；失败跳过。
  - 保持批量就地刷新逻辑。

## 验证
- 进入房间管理时不再出现 default-cover.png 的多次请求。
- 页面仅渲染 Pinia 中的真实房间数据；无占位导致的空封面与错误链路。