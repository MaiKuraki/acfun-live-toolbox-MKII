## 问题定位
- “直播中”文案位于顶栏房间状态区 `roomStatusText`，点击应打开右侧房间状态抽屉。
- 当前抽屉绑定为 `v-model="showRoomDrawer"`（packages/renderer/src/components/Topbar.vue:130），TDesign Drawer 在 Vue3 规范下应使用 `v-model:visible`。
- 与同文件中的 `t-popup` 使用 `v-model:visible`（packages/renderer/src/components/Topbar.vue:44-48）一致，说明 Drawer 使用方式不统一导致无法显示。

## 变更方案
- 将 `<t-drawer v-model="showRoomDrawer" ...>` 修改为 `<t-drawer v-model:visible="showRoomDrawer" ...>`（packages/renderer/src/components/Topbar.vue:130-135）。
- 保持点击绑定 `@click="toggleRoomDrawer"` 不变（packages/renderer/src/components/Topbar.vue:19）。
- 保持 `showRoomDrawer`、`toggleRoomDrawer` 逻辑和样式 `-webkit-app-region: no-drag`（packages/renderer/src/components/Topbar.vue:372-381）不变，避免拖拽区域拦截点击。

## 验证步骤
- 进行静态类型检查与构建检查，确保无语法/类型错误。
- 手动验证交互：
  - 点击顶栏房间状态区（显示“X个直播中/全部离线/无房间”）应打开右侧抽屉并展示房间列表。
  - 在抽屉内点击房间项的跳转按钮应触发 `router.push({ path: '/live/room', query: { roomId } })`（packages/renderer/src/components/Topbar.vue:249-257）。
  - 关闭抽屉后再次点击应正确切换显示。

## 回归检查
- 文案显示：无房间/全部离线/`X个直播中`（packages/renderer/src/components/Topbar.vue:206-211）。
- 拖拽与点击区域互不影响：顶栏整体拖拽（packages/renderer/src/components/Topbar.vue:296-307）与 `room-status` 的 `no-drag`（packages/renderer/src/components/Topbar.vue:380-381）。
- 保证无冗余代码与注释，变更后进行一次文件内无用符号检查。

请确认上述计划，我将按此修改并完成验证。