**问题可能性**
- 当前 `system.getBuildInfo` 候选路径仍与实际运行环境不一致（Electron 开发/打包目录差异），导致未命中根 `package.json`。

**修复方案**
1. 增强主进程 `system.getBuildInfo` 的定位策略：
   - 以 `app.getAppPath()` 为起点，向上递归查找父目录，逐层检测 `package.json`：
     - 解析出 `name === 'ACLiveFrame'` 且存在 `version` 即视为根包；取其 `version` 与 `mtimeMs`。
   - 若递归未命中，再尝试 `process.cwd()/package.json`。
   - 最后保底：返回 `app.getVersion()` 与 `Date.now()`。

2. 渲染层设置页：
   - 仅调用 `system.getBuildInfo` 并使用返回值；失败时显示“未知”。
   - 在控制台打印返回数据，便于快速核实（不影响用户体验）。

3. 验证与类型检查：
   - 确认关于页显示版本为 `3.1.0`，构建时间为根 `package.json` 修改时间。
   - 运行类型检查确保无回归。

确认后我将按以上步骤修改主进程与设置页调用，并验证生效。