## 目标
- 在 `startDanmuService` 的回调处直接打印收到的入站事件信息，确保即使未路由也能看到事件到达主进程。
- 打印内容包含事件类型、键名、关键字段，帮助定位后续路由问题。

## 变更
- 修改 `packages/main/src/adapter/AcfunAdapter.ts`：在 `this.api.danmu.startDanmu(..., (event) => { ... })` 的回调体最前面增加 `console.info` 打印：
  - `[Adapter] callback event type=<...> keys=<...> user=<...>(<...>) content="..." ts=<...>`
- 保留现有路由逻辑（`handleDanmuEvent(event)`），以便继续触发 RoomManager → EventWriter。

## 验证
- 触发评论或其它类型后，首先应看到 `[Adapter] callback event ...`；随后看到 `[Room] unified ...` 与 `[DB] insert ...` 和 `[Writer] commit ok count=...]`。
- 若仅出现回调日志而无统一事件或写库日志，贴该行内容我将依据实际结构继续完善路由。