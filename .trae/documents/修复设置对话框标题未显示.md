## 目标
- 让“房间设置”对话框显示标题，无论动态还是静态都正确渲染。

## 修改
- 在 `t-dialog` 中使用 `header` 插槽渲染标题：`<template #header>{{ settingsRoomTitle }}</template>`，避免对 `title` 属性兼容性导致的空标题。
- 保持既有动态标题逻辑 `settingsRoomTitle`，无房间时回退为“房间设置”。

## 变更位置
- 文件：`packages/renderer/src/pages/LiveRoomPage.vue`
- 节点：房间设置对话框（约 271–287 行）

## 验证
- 打开设置对话框可见标题；切换不同房间时标题实时更新；关闭后不影响其它对话框标题。