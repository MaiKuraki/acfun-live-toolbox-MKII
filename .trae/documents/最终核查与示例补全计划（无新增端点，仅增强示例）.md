## 结论
- 已覆盖主进程实现的所有 /api、/sse、/plugins 以及包装页端点；未发现遗漏的接口。
- 不记录未实现的 /api/diagnostics，确保文档与代码一致。

## 补充（增强示例与可复制演示）
- Overlay REST 增加成功示例与 curl：
  - GET /api/overlay/:overlayId → 返回 overlay/room/token/websocket_endpoint 的示例
  - POST /api/overlay/:overlayId/action → 成功 { success: true } 示例
  - POST /api/overlay/:overlayId/send → 成功 { success: true } 示例
  - POST /api/overlay/create → 成功返回 overlayId 示例
- 插件 Overlay 消息中心：
  - POST /api/plugins/:pluginId/overlay/messages → 单发（返回 id）与广播（返回 count/ids）两组 curl 示例
- 只读仓库上报：
  - POST /api/renderer/readonly-store → readonly-store-init 与 readonly-store-update 两种成功示例
- SSE 插件 Overlay：
  - GET /sse/plugins/:pluginId/overlay → Last-Event-ID 使用示例（curl 带 Header）

## 执行
- 更新 docs/apidoc-src/endpoints.apidoc.js 增加 @apiSuccessExample 与 @apiExample（curl）；严格按 ApiServer/AcfunApiProxy 的返回结构填充，不虚构字段。
- 运行 pnpm docs:api 生成并验证页面。