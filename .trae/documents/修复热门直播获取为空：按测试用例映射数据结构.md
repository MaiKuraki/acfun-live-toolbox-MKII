## 结论
- 测试文件显示 getHotLives 返回结构为 `{ success, data: { lives: Live[], total } }`，房间 ID 字段为 `liveId`。
- 当前渲染层将 `res.data` 误当成数组或查找 `list/items`，导致无法取到 `data.lives`，下拉为空。

## 修改点
- 文件：`packages/renderer/src/pages/LiveRoomPage.vue`
- 函数 `fetchHotLives()`：将列表解析改为 `const list = Array.isArray(res.data?.lives) ? res.data.lives : []`。
- 选项映射 `hotOptions`：使用 `live.liveId`、`live.title`、`live.streamer?.userName`、`live.onlineCount` 生成 `{ label, value }`。
- 保持 `t-select` 的 `filterable/clearable/creatable` 与打开/聚焦拉取逻辑不变。
- 在提交前保留 `validateRoomId()` 的调用，支持从链接提取房间ID。

## 验证
- 运行渲染包类型检查，确保 TS 正常。
- 静态检查：热门直播下拉显示条目且选择后填入房间ID。

## 说明与建议
- 若仍为空，需确认主进程 API 服务端口。当前 Preload 默认端口为 `18299`，API 服务器默认 `1299`。如未配置环境变量 `ACFRAME_API_PORT`，可能请求不到服务；必要时我可同步端口或在 Preload 读取实际端口。