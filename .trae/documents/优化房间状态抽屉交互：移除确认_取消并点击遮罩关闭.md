## 目标
- 顶栏房间状态抽屉不显示确认/取消按钮。
- 点击抽屉的遮罩层（modal overlay）自动关闭抽屉。

## 变更点
- 文件：`packages/renderer/src/components/Topbar.vue`
- 位置：`<t-drawer ...>`（packages/renderer/src/components/Topbar.vue:130-135）
- 修改：
  - 增加 `:footer="false"` 以隐藏底部操作区（确认/取消）。
  - 增加 `:closeOnOverlayClick="true"` 以启用点击遮罩关闭抽屉。
  - 保持现有 `v-model:visible="showRoomDrawer"` 以及 `@click="toggleRoomDrawer"` 不变（packages/renderer/src/components/Topbar.vue:18-21）。

## 验证
- 静态类型检查与构建检查通过。
- 手动验证：
  - 抽屉不再显示确认/取消按钮。
  - 点击遮罩层会关闭抽屉，`showRoomDrawer` 与 `v-model:visible` 同步为 `false`。
  - 抽屉内房间跳转按钮仍正常跳转（packages/renderer/src/components/Topbar.vue:174-179, 249-258）。

## 说明
- 不改动抽屉内容、样式以及拖拽/点击区域的既有约束。