## 问题
- `packages/main/src/index.ts:186-198` 调用 `initializeIpcHandlers(...)` 传入了 11 个参数（包含 `databaseManager`），而函数签名需要 10 个。导致 typecheck 报错 TS2554。

## 修改
- 删除多余的 `databaseManager` 作为第 11 个参数，仅保留前 10 个参数，与函数签名一致。

## 验证
- 重新运行类型检查，应无该错误。其余代码逻辑不变。