## 目标
- 使“更多”按钮与其他按钮保持同一行，对齐且不撑宽。

## 修改
- 模板：给“更多”按钮添加 `shape="square"` 与 `class="more-btn"`。
- 样式：
  - `.room-actions { display: grid; grid-template-columns: minmax(96px, auto) minmax(96px, auto) 28px; gap: 8px; align-items: center; }`
  - `.more-btn { padding: 0; width: 28px; min-width: 28px; height: 28px; }`
  - `.more-btn :deep(.t-button__text) { display: flex; align-items: center; justify-content: center; }`
  - `.more-btn .t-icon { font-size: 16px; }`

## 验证
- 类型检查通过。
- 更多按钮宽度固定，不再导致换行或不对齐；三按钮稳定同行显示。